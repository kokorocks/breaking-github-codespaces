# breaking-github-codespaces

first command is
```bash
pkill -f code
```

and 

```bash
sudo apt update
sudo apt install firefox
```

or

```bash
sudo apt update
sudo apt install ubuntu-desktop
sudo reboot
```
